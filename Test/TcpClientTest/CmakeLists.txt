SET(PROJECT_NAME "TcpClientTest")
project(${PROJECT_NAME})


add_package(src ./src)
add_package(SocketInit ${CMAKE_SOURCE_DIR}/Source/SocketInit)

message("SocketInit" ${CMAKE_SOURCE_DIR}/Source/SocketInit)

add_executable(${PROJECT_NAME} ${all_file})

